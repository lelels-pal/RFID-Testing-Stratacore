# Local development
NEXT_PUBLIC_BACKEND_URL=http://localhost:4001

# Production (stratacore.tech)
# NEXT_PUBLIC_BACKEND_URL=https://api.stratacore.tech

# Chargers shown in operator portal (must match OCPP connection IDs)
NEXT_PUBLIC_CHARGERS=[{"chargerId":"DELTA123","connectorId":1}]

# Operator login: RFID or username + PIN (configured in kiosk Operators tab)
